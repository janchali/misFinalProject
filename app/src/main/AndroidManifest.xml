<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omercancali.misfinalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MisFinalProject">
        <activity android:name=".Store"></activity>
        <activity android:name=".save" />
        <activity android:name=".DriverStandings" />
        <activity android:name=".Details" />
        <activity android:name=".Teams" />
        <activity android:name=".Drivers" />
        <activity android:name=".RaceCalendar" />
        <activity android:name=".TeamStandings" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>